node_modules/
.expo/
dist/

# Native projects ARE tracked (not the default Expo managed-workflow
# assumption) since we've made manual edits beyond what `expo prebuild`
# regenerates from config alone (Podfile fix, patched OCR module, etc.) —
# only their regeneratable build output/dependencies are excluded.
ios/Pods/
ios/build/
android/build/
android/app/build/
android/.gradle/

*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/
.env
.env*.local

# Personal Claude Code permission overrides, not shared team config.
.claude/settings.local.json

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli